html
    head
        title= exercise.name
    body(class=config.theme)
        div#navbar-container
            include partials/navbar
        div#main-container
            p
                =exercise.assignment.name 
                |  &ndash; 
                =exercise.name
            block content
                canvas#content
            block assignment
