html
    head
        title= exercise.name
    body(class=config.theme)
        div#navbar-container
            include partials/navbar
        div#main-container
            h2
                =exercise.assignment.name 
                |  &ndash; 
                =exercise.name
            block content
                canvas#content
            block assignment
