'use strict';

module.exports = {
    alias: ['c'],
    desc:
        'Config file to use. ' +
        'Allows to re-use the framework for multiple lectures.',
    type: 'string',
    default: './config.json'
};
